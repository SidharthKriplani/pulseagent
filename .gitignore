# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# Cache — committed so Cloud Run loads in ~15s instead of rebuilding from scratch
# retrieval_index.pkl is excluded (old monolithic format, replaced by 3-part cache)
.cache/retrieval_index.pkl

# Eval evidence (per-query JSONs, regenerate with eval_runner.py)
# outputs/evidence/ is gitignored; outputs/eval_summary.json IS committed (proof of eval)
outputs/evidence/

# Session/tooling directories
.codex/

# macOS
.DS_Store
.AppleDouble

# IDE
.idea/
.vscode/
*.swp
.codex/
